{
  "name": "app-template",
  "version": "0.1.0",
  "private": true,
  "description": "Template repository for the Rhymion App Generator. Edit prj/, then deploy.",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "sync": "bash scripts/sync-prj.sh",
    "dev": "bash scripts/sync-prj.sh && npm --prefix app-generator run dev:all",
    "build": "bash scripts/sync-prj.sh && npm --prefix app-generator run build:all",
    "start": "npm --prefix app-generator run start",
    "cleanup": "npm --prefix app-generator run cleanup:all",
    "deploy": "bash scripts/sync-prj.sh && vercel",
    "deploy:prod": "bash scripts/sync-prj.sh && vercel --prod"
  }
}
