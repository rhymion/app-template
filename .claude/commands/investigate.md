---
description: Investigate code or answer a question in proj_c — no edits, no gates.
argument-hint: <question or topic>
---

This is an **investigate** task for proj_c.

Refer to `@app-generator/.claude/commands/investigate.md` for the full procedure.

Task: $ARGUMENTS

## proj_c constraints

- **Read**: both `prj/` and `app-generator/` are in scope for reading.
- **Write**: `prj/` only (investigation notes, if any output is needed).
- Do **not** modify `app-generator/`.
