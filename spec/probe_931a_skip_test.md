# Probe: doc-only E2E skip verification (subtask_931a)

Disposable probe file used to empirically verify that a spec/**-only diff
causes the e2e-tests job to show as Skipped in CI. Not part of the real
product docs; the probe PR that carries this file will be closed and its
branch deleted immediately after the measurement.
