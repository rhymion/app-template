This is an **update-code** task for proj_c. Read AGENTS.md and `app-generator/AGENTS.md` before starting.

For the completion gate, see `app-generator/.claude/commands/update-code.md §Completion gate` — that file is the source of truth. See also `app-generator/.codex/prompts/update-code.md` for the full rules.

## proj_c constraints

- Edit files under `prj/` only.
- Do not touch `app-generator/` source files.
- Do not edit anything outside `prj/`.

## Input
$ARGUMENTS
